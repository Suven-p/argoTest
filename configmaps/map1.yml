apiVersion: v1
kind: ConfigMap
metadata:
  name: test-configmap
  namespace: argo-test
metadata:
  name: second-test-configmap
data:
  # Configuration values can be set as key-value properties
  database: mongodb
  database_uri: mongodb://localhost:27017
